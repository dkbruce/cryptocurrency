CREATE TABLE bitcoin(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE dash(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE ethereum(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
	PRIMARY KEY(`date`)
);

CREATE TABLE iota(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE litecoin(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE monero(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE neo(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
	PRIMARY KEY(`date`)
);

CREATE TABLE nem(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE numeraire(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE omisego(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE qtum(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE ripple(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE stratis(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE waves(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    market_cap BIGINT,
    PRIMARY KEY(`date`)
);

CREATE TABLE sp(
	`date` DATE,
    `open` REAL,
    high REAL,
    low REAL,
    `close` REAL,
    volume BIGINT,
    PRIMARY KEY(`date`)
);